import { createComponent } from "../createComponent";
import * as React from "react";
import { Bar as BarElement } from "../components/Bar";

export const Bar = createComponent(React, "bar-element", BarElement, {});
